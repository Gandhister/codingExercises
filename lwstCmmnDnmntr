# calculate the lowest common denonminator

denominator1 = 37
denominator2 = 44

def lwstCmmnDnmntr(denom1, denom2):
    found_denom = False
    multiple = 0
    while found_denom == False:
        multiple += 1
        # trial1 = 
        trial2 = multiple * denom2
        if trial2 % denom1 == 0:
            print('found a common denominator when trial2 equals {}'.format(trial2))
            found_denom = True
            # break

lwstCmmnDnmntr(denominator1, denominator2)

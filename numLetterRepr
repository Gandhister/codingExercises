"""
Given a mapping of digits to letters (as in a phone number), and a digit string, return all possible letters the number 
could represent. You can assume each valid number in the mapping is a single digit.

For example if {“2”: [“a”, “b”, “c”], 3: [“d”, “e”, “f”], …} then “23” should return 
[“ad”, “ae”, “af”, “bd”, “be”, “bf”, “cd”, “ce”, “cf"].

"""

numLetterMapping = {2: ["a", "b", "c"], 3: ["d", "e", "f"], 4: ["g", "h", "i"]}

def numLetterRepr(ensemble, number):
    temp_string = ""
    numeri = str(number)
    for i in range(0, len(ensemble[int(numeri[0])])):
        for j in range(0, len(ensemble[int(numeri[1])])): # len(ensemble[int(numero)]) -1 
            temp_string = ensemble[int(numeri[0])][i] + ensemble[int(numeri[1])][j]
            output_list.append(temp_string)
    print(output_list)

print(numLetterMapping[2][0] + numLetterMapping[3][0] + "\n" + numLetterMapping[2][0] + numLetterMapping[3][1] 
      + "\n ... \n" + numLetterMapping[2][2] + numLetterMapping[3][2]) 
numLetterRepr(numLetterMapping, 23)
